import { expect } from 'chai';
import rawly from '../lib';

describe('rawly', () => {
  it('should have unit test!', () => {
    expect(false, 'we expected this package author to add actual unit tests.');
  });
});
